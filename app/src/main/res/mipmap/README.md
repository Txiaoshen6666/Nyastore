# Launcher icon
Generate an adaptive launcher icon via **File → New → Image Asset** in Android Studio; output goes to `mipmap-anydpi-v26/`, `mipmap-hdpi/`, etc. The Manifest references `ic_launcher` / `ic_launcher_round`.
